apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: longhorn
  namespace: longhorn-system
spec:
  parentRefs:
    - name: internal-gateway
      namespace: default
      sectionName: https
  hostnames:
    - "longhorn.local.fresh96jwjw.org"
  rules:
    - backendRefs:
        - name: longhorn-frontend
          port: 80
          weight: 1
