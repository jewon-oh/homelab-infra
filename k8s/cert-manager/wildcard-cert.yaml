apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homelab-wildcard-cert
  namespace: default
spec:
  secretName: homelab-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - "*.local.fresh96jwjw.org"
    - "*.fresh96jwjw.org"
    - "fresh96jwjw.org"
