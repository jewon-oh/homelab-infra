apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: headlamp
  namespace: headlamp
spec:
  parentRefs:
    - name: internal-gateway
      namespace: default
      sectionName: https
  hostnames:
    - "headlamp.local.fresh96jwjw.org"
  rules:
    - backendRefs:
        - name: headlamp
          port: 80
